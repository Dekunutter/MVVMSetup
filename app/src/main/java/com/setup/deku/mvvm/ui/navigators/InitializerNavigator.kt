package com.setup.deku.mvvm.ui.navigators

/**
 * Navigation interface that declares which activities the initializer activity can open
 */
interface InitializerNavigator {
    fun openCounterView()
}